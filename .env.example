# ================================================================
#  ConstructaX — Environment Variables Template
#  Copy this file to .env and fill in your real values
#  NEVER commit .env to version control!
# ================================================================

# ── Firebase Configuration ──────────────────────────────────────
# Firebase Console → Project Settings → General → Your apps → SDK snippet
VITE_FIREBASE_API_KEY=your_firebase_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
VITE_FIREBASE_APP_ID=1:your_sender_id:web:your_app_id

# ── Cloudinary Configuration ────────────────────────────────────
# Cloudinary Dashboard → Settings → Upload → Upload Presets (must be UNSIGNED)
VITE_CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
VITE_CLOUDINARY_UPLOAD_PRESET=your_unsigned_upload_preset

# ================================================================
#  HOW TO SET THESE IN NETLIFY:
#
#  1. Go to your Netlify dashboard → Select your site
#  2. Site configuration → Environment variables
#  3. Click "Add a variable" for each variable above
#  4. Paste the key (e.g. VITE_FIREBASE_API_KEY) and value
#  5. Set scope: "All scopes" (Production + Deploy Previews)
#  6. Save → Trigger a new deploy
#
#  IMPORTANT:
#  • All Vite env vars MUST start with VITE_ to be bundled
#  • Do NOT add these as server-only vars in Netlify (they are
#    build-time vars used by the React bundle, not serverless fns)
# ================================================================
